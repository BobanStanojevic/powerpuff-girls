import * as React from 'react';

const ErrorMessage = () => (
	<div className="m-errorMessage">
		<h1>Oups something went wrong, please retry again later</h1>
	</div>
);

export default ErrorMessage;
